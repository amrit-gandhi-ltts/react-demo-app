import React from 'react';

const GuestGreeting = () => {    
    return (
        <div>
            Welcome user, Login to continue.
        </div>
    );   
}

export default GuestGreeting;